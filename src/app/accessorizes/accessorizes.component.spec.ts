/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AccessorizesComponent } from './accessorizes.component';

describe('AccessorizesComponent', () => {
  let component: AccessorizesComponent;
  let fixture: ComponentFixture<AccessorizesComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AccessorizesComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(AccessorizesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/